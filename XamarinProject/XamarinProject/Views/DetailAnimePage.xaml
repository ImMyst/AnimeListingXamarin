<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinProject.Views.DetailAnimePage"
             BackgroundImage="background.jpg"
             Title="{Binding Titre}">

    <ContentPage.Content>
            <StackLayout Margin="20, 10, 20, 20">
                <Label Text="{Binding CurrentAnime.Name}" 
                       TextColor="#ff4a32"
                       FontSize="35"/>
                <Label Text="{Binding CurrentAnime.Studio}" 
                       TextColor="#85ffffff"
                       FontSize="25"/>
            <ScrollView>
                <Label Margin="20"
                       Text="{Binding CurrentAnime.Summary}" 
                       TextColor="#ffffff"
                       FontSize="20"/>
            </ScrollView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>